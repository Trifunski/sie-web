<div data-aos="fade-up" class="bg-white shadow-xl p-6 text-center rounded-xl">
	<div style="background-color: {{ $bgColor }}" class="rounded-full w-16 h-16 flex items-center justify-center mx-auto shadow-lg transform -translate-y-12">
        {!! $svg !!}
	</div>
	<h1 class="font-medium text-xl mb-3 lg:px-14 text-darken">{{ $title }}</h1>
	<p class="px-4 text-gray-500">{{ $description }}</p>
</div>